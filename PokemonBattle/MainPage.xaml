<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PokemonBattle.MainPage">

    <Grid Margin="25" ColumnSpacing="10" >
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="2*" />
        </Grid.ColumnDefinitions>

        <Button
    Grid.Row="4"
    Grid.Column="2"
    Text="start!"
            TextTransform="Uppercase"
            CornerRadius="0"
            WidthRequest="250"
            HeightRequest="100"
    Command="{Binding GoToTeamBuilderPageCommand}"
    Background="Bisque"
    HorizontalOptions="Fill"
    ZIndex="1" />
        <Image
               Grid.Row="0"
    Grid.RowSpan="5"
    Grid.Column="0"
    Grid.ColumnSpan="5"
    Source="bigolproject.png"
             WidthRequest="900"
    HeightRequest="900"
    HorizontalOptions="Center"
    VerticalOptions="Center"
    Aspect="AspectFit"/>

    </Grid>
</ContentPage>
